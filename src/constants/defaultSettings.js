export const DEFAULT_SETTINGS = Object.freeze({
  lineJoin: 'round',
  lineCap: 'round',
  strokeStyle: '#000',
  lineWidth: 2,
  globalCompositeOperation: 'source-over',
  width: 720,
  height: 400,
});
